type options = {
  className: string,
  closeButton: bool,
  offset: array(int),
  closeOnClick: bool,
};

type popupOptions = {
  class_name: option(string),
  close_button: option(bool),
  offset: option(array(int)),
  close_on_click: option(bool),
};
