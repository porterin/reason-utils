[@react.component]
let make = (): React.element => {
  <TooltipComponent content={"Balance will be updated after transaction success Balance will be updated after transaction success Balance will be updated after transaction success Balance will be updated after transaction success"}>
    <div>{React.string("Test ToolTip")}</div>
  </TooltipComponent>
}
